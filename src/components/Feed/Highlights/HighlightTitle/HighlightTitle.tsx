import React from "react";
import styles from "../Highlights.module.css";

const HighlightTitle: React.FC = () => {
  return <p className={styles.userName}>usaswim...</p>;
};

export default HighlightTitle;
